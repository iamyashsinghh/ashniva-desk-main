import { Module } from '@nestjs/common';

/**
 * GitHub App installation per organization, repository ↔ project mapping with branch roles and required checks, signed and idempotent webhooks, linking branches/commits/PRs/checks/deployments to tasks by AD-### references.
 * Module boundary only (Phase 0). Implementation planned for Phase 2 (order 7) — see README.md.
 */
@Module({})
export class GithubModule {}
