# Ashniva Desk web app — copy to apps/web/.env
# Base URL of the API. In local development the Vite dev server proxies /api to localhost:3000,
# so the relative default works without any change.
VITE_API_BASE_URL=/api/v1
